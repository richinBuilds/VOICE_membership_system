spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: false
  # Disable mail in tests to avoid connection errors
  mail:
    host: smtp.gmail.com
    port: 587
    username: tarparakrimy@gmail.com
    password: afpe ztfj lpfc ukud
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    org.springframework.security: DEBUG
    org.voice.membership: DEBUG
